class ApiConfig {
  static const String baseUrl = 'https://api.your-hr-server.com';
  static const Duration connectTimeout = Duration(seconds: 10);
  static const Duration receiveTimeout = Duration(seconds: 20);
}